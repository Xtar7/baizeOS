# PRODUCT.md — baizeOS

## 产品一句话

baizeOS（白泽）是跑在本机 GPU 上的 RAG 知识库问答工具：用户把 .txt/.md/.pdf 文档喂进「知识库」，然后像 LM Studio 一样在本地与模型对话，回答附带可溯源的引用片段。对标 LM Studio 的体验，但专注「知识库管理 + AI 对话」两条主线。

## 访问者与场景

个人开发者 / 知识工作者，在自己的 Windows/Mac 电脑上使用。场景是深夜书桌前的单人工具：隐私敏感（数据不出本机）、需要等待模型推理（流式反馈必须诚实可见）、中英文混用（界面为简体中文）。

## 访客模式

**Operate** —— 访问者完成任务：发起对话、管理知识库与附件、检查模型状态。扫描效率、状态清晰度、一致性优先于表达；品牌感落在精确的细节里。

## 核心任务（按频率）

1. 对话提问（普通 / RAG 模式，流式输出，查看引用来源）
2. 管理知识库：创建、编辑（名称/描述/提示词/嵌入模型）、删除
3. 文件管理：向知识库上传文档、批量删除、重建向量索引
4. 聊天附件：随对话上传临时文件作为上下文
5. 设置：查看本地 LLM / Embedding 模型、切换明暗主题

## 硬约束

- 后端 Flask 运行于 localhost:5000，前端经 Vite 代理访问 `/v1/*`；所有离线失败态都要给出明确恢复指引。
- 接口契约以 `docs/API调用方案.md` 与 `docs/接口规范.md` 为准（DELETE+body、POST list/delete、form 字段名 `file` 等），不得偏离。
- 重建向量索引与上传向量化均为耗时操作，UI 必须有进度/加载状态，禁止无反馈等待。
- 中文输入法安全：Enter 相关交互须规避 IME 组合期误触（发送绑定 Ctrl+Enter）。

## 视觉方向（用户指定，2026-08）

参照 Claude 的设计语言：暖象牙底色、暖炭墨文字、唯一强调色陶土橙；细线边框承担结构层级，阴影只给浮层；衬线体仅用于品牌字标与问候语等「助手声音」时刻；细线内联 SVG 图标系统。文档中的 Apple 风格描述已被用户明确忽略。
